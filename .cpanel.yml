---
deployment:
  tasks:
    # ১. ডিপ্লয়মেন্ট পাথ ডিফাইন করা
    - export DEPLOYPATH=/home/panaroma/shitalakshyafoundation.com

    # ২. নিশ্চিত করা যেন টার্গেট ডিরেক্টরি থাকে
    - mkdir -p $DEPLOYPATH

    # ৩. শুধুমাত্র প্রয়োজনীয় ফাইল কপি করা (.git ফোল্ডার বাদ দিয়ে)
    - /usr/bin/rsync -av --exclude='.git' . $DEPLOYPATH

    # ৪. কম্পোজার ডিপেনডেন্সি ইনস্টল করা
    - cd $DEPLOYPATH && composer install --no-dev --optimize-autoloader

    # ৫. স্টোরেজ লিংক ও পারমিশন নিশ্চিত করা
    - cd $DEPLOYPATH && php artisan storage:link || true
    - cd $DEPLOYPATH && chmod -R 775 storage bootstrap/cache || true

    # ৬. লারাভেল ক্যাশ ক্লিয়ার ও অপটিমাইজ করা
    - cd $DEPLOYPATH && php artisan optimize:clear
    - cd $DEPLOYPATH && php artisan config:cache
    - cd $DEPLOYPATH && php artisan route:cache
    - cd $DEPLOYPATH && php artisan view:cache

    # ৭. ডাটাবেস মাইগ্রেশন (force ফ্ল্যাগ সহ)
    - cd $DEPLOYPATH && php artisan migrate --force